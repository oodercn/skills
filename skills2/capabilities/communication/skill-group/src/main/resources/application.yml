server:
  port: 8087

spring:
  application:
    name: skill-group

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

skill:
  group:
    max-groups-per-user: 100
    max-members-per-group: 500
