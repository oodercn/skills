server:
  port: 8090

spring:
  application:
    name: skill-hosting

management:
  endpoints:
    web:
      exposure:
        include: health,info

hosting:
  max-instances: 100
  max-cpu-cores: 16
  max-memory-mb: 32768
