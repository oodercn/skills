server:
  port: 8080

spring:
  application:
    name: skill-ui-test
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

nexus:
  skills:
    path: ./skills
  menu:
    config:
      path: ./menu-config.json
  skill-index:
    path: ./skill-index.yaml

logging:
  level:
    net.ooder: DEBUG
    org.springframework: INFO
