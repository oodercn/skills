apiVersion: skill.ooder.net/v1
kind: Skill

metadata:
  id: skill-health
  name: 健康检查服�?  version: 1.0.0
  description: 系统健康检查、服务状态监控、报告生�?  author: ooder Team
  type: system-service
  license: Apache-2.0
  homepage: https://gitee.com/ooderCN/skills
  keywords:
    - health
    - check
    - monitor
    - service

spec:
  type: system-service
  
  dependencies: []
  
  providedInterfaces:
    - id: health-check
      version: "1.0"
      description: "健康检查接�?
    - id: service-status
      version: "1.0"
      description: "服务状态接�?
  
  capabilities:
    - id: health-check
      name: 健康检�?      description: 执行系统健康检�?      category: monitoring
    - id: service-check
      name: 服务检�?      description: 检查各服务状�?      category: monitoring
    - id: report-generation
      name: 报告生成
      description: 生成健康报告
      category: monitoring
  
  endpoints:
    - path: /api/health/check
      method: POST
      description: 执行健康检�?      capability: health-check
    - path: /api/health/services
      method: GET
      description: 获取服务列表
      capability: service-check
    - path: /api/health/services/{serviceName}/check
      method: POST
      description: 检查指定服�?      capability: service-check
    - path: /api/health/report
      method: POST
      description: 生成健康报告
      capability: report-generation
    - path: /api/health/schedule
      method: POST
      description: 调度健康检�?      capability: health-check
    - path: /api/health/status
      method: GET
      description: 获取整体健康状�?      capability: health-check
  
  runtime:
    language: java
    javaVersion: "8"
    framework: spring-boot
    mainClass: net.ooder.skill.health.HealthSkillApplication
  
  config:
    optional:
      - name: CHECK_INTERVAL
        type: integer
        default: 60000
        description: 检查间隔（毫秒�?      - name: TIMEOUT
        type: integer
        default: 5000
        description: 超时时间（毫秒）
      - name: RETRY_COUNT
        type: integer
        default: 3
        description: 重试次数
  
  resources:
    cpu: "100m"
    memory: "128Mi"
    storage: "50Mi"
  
  offline:
    enabled: true
    cacheStrategy: local
    syncOnReconnect: true
