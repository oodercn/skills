server:
  port: 8086

spring:
  application:
    name: skill-health

management:
  endpoints:
    web:
      exposure:
        include: health,info

health:
  check-interval: 60000
  timeout: 30000
  max-concurrent: 10
