apiVersion: skill.ooder.net/v1
kind: SkillIndexEntry

metadata:
  id: skill-meeting-minutes
  name: 会议纪要场景能力
  version: "2.3.0"
  description: "会议纪要场景能力，支持会议记录、摘要生成、任务提取"

spec:
  skillForm: SCENE
  sceneType: TRIGGER
  visibility: public
  businessCategory: OFFICE_COLLABORATION
  subCategory: meeting
  category: WORKFLOW
  capabilityCategory: util
  
  capabilityAddresses:
    required:
      - address: 0x30
        name: LLM_BASE
        description: "LLM基础服务"
        fallback: 0x31
      - address: 0x50
        name: COMM_BASE
        description: "通讯基础服务"
    optional: []
  
  tags:
    - meeting
    - minutes
    - summary
    - scene-capability
  
  dependencies:
    - skill-llm-conversation
    - skill-msg
  
  roles:
    - name: MANAGER
      displayName: 场景管理员
      minCount: 1
      maxCount: 1
      permissions: [READ, WRITE, CONFIG, DELETE]
    - name: MEMBER
      displayName: 场景成员
      minCount: 0
      maxCount: 100
      permissions: [READ, WRITE]
