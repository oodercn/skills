apiVersion: skill.ooder.net/v1
kind: SkillIndexEntry

metadata:
  id: skill-task
  name: 任务管理服务
  version: "0.7.3"
  description: "任务管理服务，提供任务创建、执行、状态管理"

spec:
  skillForm: PROVIDER
  visibility: internal
  businessCategory: INFRASTRUCTURE
  subCategory: task
  category: SERVICE
  capabilityCategory: sched
  
  capabilityAddresses:
    required:
      - address: 0x70
        name: SCHED_BASE
        description: "调度基础服务"
    optional: []
  
  tags:
    - task
    - job
    - scheduler
  
  dependencies:
    - skill-scheduler-quartz
