server:
  port: 8086

spring:
  application:
    name: skill-llm-config-manager

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    net.ooder.skill.llm.config: DEBUG
