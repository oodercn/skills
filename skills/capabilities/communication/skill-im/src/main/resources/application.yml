server:
  port: 8086

spring:
  application:
    name: skill-im

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

skill:
  im:
    max-conversations: 1000
    max-contacts: 5000
