apiVersion: skill.ooder.net/v1
kind: SkillIndexEntry

metadata:
  id: skill-user-auth
  name: 用户认证服务
  version: "0.7.3"
  description: "用户认证服务，提供用户身份验证、令牌管理功能"

spec:
  skillForm: PROVIDER
  visibility: internal
  businessCategory: SECURITY_AUDIT
  subCategory: auth
  category: SERVICE
  capabilityCategory: auth
  
  capabilityAddresses:
    required:
      - address: 0x10
        name: AUTH_USER
        description: "用户认证服务"
      - address: 0x11
        name: AUTH_TOKEN
        description: "令牌管理服务"
    optional: []
  
  tags:
    - auth
    - user
    - token
    - security
  
  dependencies: []
