server:
  port: 8088

spring:
  application:
    name: skill-health

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

skill:
  health:
    check-interval: 60000
    timeout: 5000
