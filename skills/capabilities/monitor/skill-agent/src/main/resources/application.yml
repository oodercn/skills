server:
  port: 8089

spring:
  application:
    name: skill-agent

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

skill:
  agent:
    heartbeat-interval: 30000
    offline-threshold: 90000
