server:
  port: 8091

spring:
  application:
    name: skill-audit

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

skill:
  audit:
    retention-days: 90
    max-query-size: 10000
