server:
  port: 8085

spring:
  application:
    name: skill-capability

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    net.ooder: DEBUG
