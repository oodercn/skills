# CommonConfig fallback - ensures CommonConfig.props is not null
# This file exists so that ClassUtility.loadResource("application.properties") succeeds
# Actual Spring Boot config is in application.yml
