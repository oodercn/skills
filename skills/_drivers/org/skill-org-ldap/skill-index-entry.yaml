apiVersion: skill.ooder.net/v1
kind: SkillIndexEntry

metadata:
  id: skill-org-ldap
  name: LDAP组织服务
  version: "0.7.3"
  description: "LDAP组织服务，提供基于LDAP的组织架构管理和用户认证"

spec:
  skillForm: DRIVER
  visibility: developer
  businessCategory: INFRASTRUCTURE
  subCategory: ldap
  category: SERVICE
  capabilityCategory: org
  
  capabilityAddresses:
    required:
      - address: 0x0C
        name: ORG_LDAP
        description: "LDAP组织服务"
    optional: []
  
  tags:
    - ldap
    - organization
    - auth
  
  dependencies:
    - skill-org-base
