server:
  port: 8082

spring:
  application:
    name: skill-org-dingding

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    net.ooder: DEBUG
    root: INFO
