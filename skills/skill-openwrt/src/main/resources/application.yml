server:
  port: 8090

spring:
  application:
    name: skill-openwrt

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

skill:
  openwrt:
    connection-timeout: 30000
    ssh-port: 22
    http-port: 80
