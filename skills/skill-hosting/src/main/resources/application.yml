server:
  port: 8084

spring:
  application:
    name: skill-hosting

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

skill:
  hosting:
    default-provider: kubernetes
    health-check-interval: 60000
    max-instances: 100
