server:
  port: 8081

spring:
  application:
    name: skill-a2ui

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    net.ooder: DEBUG
    root: INFO
